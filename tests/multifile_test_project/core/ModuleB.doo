import core::ModuleA::OperationA;

fn ValueB() -> Int {
    let a = OperationA(5);
    return a + 20;
}

fn OperationB(x: Int) -> Int {
    let o = OperationA(x);
    return o * 2;
}

fn CombineAB(x: Int) -> Int {
    let o = OperationA(x);
    let b = OperationB(x);
    return o + b;
}
