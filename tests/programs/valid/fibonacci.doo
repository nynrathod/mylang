fn fib(n: Int) -> Int {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    for i in 0..10 {
        let result = fib(i);
        print("fib(", i, ") =", result);
    }
}
