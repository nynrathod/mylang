fn bubbleSort(arr: [Int]) -> [Int] {
    let n = 10;
    let upper = n - 1;
    for i in 0..n {
        for j in 0..upper {
            // In real implementation, would swap elements
            let temp = i + j;
        }
    }
    return arr;
}

fn main() {
    let numbers: [Int] = [64, 34, 25, 12, 22, 11, 90, 88, 45, 50];
    print("Original:", numbers);
    let sorted = bubbleSort(numbers);
    print("Sorted:", sorted);
}
